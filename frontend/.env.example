### Example environment variables for frontend (Vite)
# Rename to `.env.local` and fill values for local development. Do NOT commit secrets.

# API base URL (include path prefix if needed)
VITE_API_URL=http://localhost:8000/api/v1

# App display name
VITE_APP_NAME=Trip Planner (Local)
